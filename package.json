{
  "name": "bitcoin-treasury-ledger",
  "version": "1.0.0",
  "private": true,
  "description": "DAT.co treasury monitor for Strategy with bilingual UI, multiple indicators, and an optional AI summary route.",
  "scripts": {
    "dev": "node scripts/dev.js",
    "start": "node server.js",
    "build": "python3 scripts/generate_dataset.py",
    "refresh:data": "python3 scripts/generate_dataset.py",
    "check": "node --check server.js && node --check scripts/dev.js && node --check api/summary.js && node --check api/redeploy.js"
  },
  "engines": {
    "node": "20.x"
  }
}
